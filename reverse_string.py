# Make a function that takes in a string and returns a reversed copy og the same string.

# Function to reverse a string

#print (s[::-1])


def reverse_string(s):
    return (s[::-1])

#print (reverse_string(s))

